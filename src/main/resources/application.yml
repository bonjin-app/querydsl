
spring:
  datasource:
    url: jdbc:mariadb://gigas.synology.me:3306/querydsl
    username: n2soft
    password: 1234

  jpa:
    hibernate:
      ddl-auto: create

    properties:
      hibernate:
        # show_sql: true
        format_sql: true
        use_sql_comments: true

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace

